{
    "name": "@cohack/example-empty",
    "private": true,
    "version": "0.0.0",
    "description": "Starter Cohack workflow project (empty template).",
    "scripts": {
        "test": "npx cohack test",
        "build": "npx cohack build",
        "deploy": "npx cohack deploy"
    },
    "dependencies": {
        "@cohack/client": "^0.0.28",
        "zod": "^4.1.12"
    }
}
