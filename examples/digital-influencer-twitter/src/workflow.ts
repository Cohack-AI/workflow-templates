import type { Client } from '@cohack/client';

export async function registerWorkflow(client: Client): Promise<void> {
    console.log('Stub digital influencer workflow.');
    console.log('Use the Cohack client to coordinate tweet generation and publishing.');
    // Placeholder reference until real logic is wired.
    void client;
}

export default registerWorkflow;
