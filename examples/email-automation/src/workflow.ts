import type { Client } from '@cohack/client';

export async function registerWorkflow(client: Client): Promise<void> {
    console.log('Stub email automation workflow.');
    console.log('Use client helpers here to integrate with Cohack and your email provider.');
    // Example placeholder so lint stays quiet.
    void client;
}

export default registerWorkflow;
